int[] foo() {
  a = b * c;
  foo(a, b);
}

int[][1][] muitasDimensoes() {
  return;
}

char testando_Comandos() {
 if (foo) { 
    while(1);
    bla = foo;
  };

  if ( (10 * 3) * i4 ) {
    foo();
  }

  bla = (10 + 2 / (10 * 3) + foo/bar) ;

  vamola = 1 == 2;

  foo();

  while (vamola + oops) {
    vamola();
  } 

  if (crap ^ 2 * 3) ;
  else {}
}

int chamadasDeFuncao() {
  potencias = 2 ^ 3 * 5 + 5 ^ 10 ^ 2;

  prioridadeSinal = -1 * -2 ^ 3;

  operadoresLogico = 1 > 2 + 3 ^ -2 * ( 2 > 3 );

  chamadaDeFuncaoNaExpr = f() + 2 ^ power() + 3 + (foo(1 + 2 * 3) / 3); 

  chamadaDeFuncaoMuitosParams = f(1, 2, foo(1, 2 ^ 3 + (3 + bar() * 5)));

  comandoFuncaoComParametro(1, 2);
}

char acessoArrays() {
  foo = array[0][foo + foo(a+2, i^2)][3];

  foo[0] = boo;
}

int x;

int blocosAninhados() {
  {
    hihi();
  } 

  foo();
}

char c1, c2;
